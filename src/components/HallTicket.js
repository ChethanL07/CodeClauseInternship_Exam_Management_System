import React from 'react';

const HallTicket = () => {
  const handleDownload = () => {
    // Logic for downloading hall ticket
  };

  return (
    <div>
      <h1>Hall Ticket</h1>
      <button onClick={handleDownload}>Download Hall Ticket</button>
    </div>
  );
};

export default HallTicket;
